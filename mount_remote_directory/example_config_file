export MOUNTBASE=$HOME/volumes # Or whatever location for mounting the remote directory
export MOUNTNAME=ig4_lustre_rr125 #Has to be the same name as config file
export REMOTE=farm5 #Remote location to access. In this case this alias is enough inside the Sanger network
export REMOTELOC=/lustre/scratch125/casm/team294rr/ig4 #Target remote directory
export MUSER=ig4
export REMOTEPORT=22
